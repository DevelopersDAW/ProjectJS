document.addEventListener("DOMContentLoaded", function () {
    console.log("HOLA");
    var addCart = document.getElementById("addCart");
    addCart.addEventListener("click",addToCart)
});

function addToCart(e){
    if(e.target.getElementById("addCart")){
        productName = 
    }
    
}